<% if answer.errors.present? %>
  $('.answer_errors').html('<%= render 'shared/errors', resource: answer %>')
<% else %>
  $('.answers <%= "#answer-id-#{answer.id}" %>').html('<%= j render answer %>')
<% end %>
